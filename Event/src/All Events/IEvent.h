#pragma once

// ----------------------------------------------------------
// -- This is the base class or, as in this case - struct. --
// -- It was created  for future  expand  the inheritance  --
// -- ierarhi                                              --
// ----------------------------------------------------------
namespace Core
{
	struct IEvent
	{
		virtual ~IEvent() {};
	};
}
// ----------------------------------------------------------